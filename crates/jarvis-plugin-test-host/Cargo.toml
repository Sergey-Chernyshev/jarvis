[package]
name = "jarvis-plugin-test-host"
version = "0.1.0"
description = "Executable in-memory contract host for Jarvis plugin tests"
license = "MIT"
edition = "2021"
rust-version = "1.77.2"

[dependencies]
jarvis-plugin-protocol = { path = "../jarvis-plugin-protocol" }
serde_json = "1"
