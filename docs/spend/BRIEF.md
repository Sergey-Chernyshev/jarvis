# Исследование: куда уходят токены

Постоянный каталог — не /tmp. Результаты класть СЮДА файлом, а не только в транскрипт:
`docs/spend/FINDINGS.md`. Каталог `/tmp/jarvis-review` уже вычистило системой вместе
со 160 скриншотами и тремя разделами отчёта — второй раз не повторяем.

## Уже посчитано (не пересчитывай, проверь и разверни)

Обмер всех транскриптов разработки Jarvis (`~/.claude/projects/-Users-pozitiv4500-Goool*`),
10 файлов с usage, 4985 ходов:

| статья | сумма | доля |
|---|---|---|
| чтение кэша | $2 918.32 | 66.6% |
| запись кэша | $1 090.30 | 24.9% |
| выход | $375.05 | 8.6% |
| свежий вход | $0.32 | 0.0% |
| **ИТОГО** | **$4 383.99** | |

- вход/выход = **401 : 1** (2 003 719 338 против 5 000 672 токенов);
- средний ход: вход 401 949, выход 1 003;
- субагенты: **0%** (isSidechain не встречается в этих сессиях);
- самая дорогая сессия a12178aa: $3 187.85, 3 240 ходов, контекст рос 33k → 608k,
  вход за сессию 1 570 286 039 токенов при 607 910 «полезного» текста —
  **один и тот же текст оплачен ~2583 раза**;
- оживление a12178aa: подняло 607 910 токенов (592 428 записью кэша), первый ход $11.13.

## Что проверить

1. **Кэш работает или теряется.** Свежий вход = 21 094 токена на 2 млрд (0.00%),
   значит кэш почти никогда не теряется в полную цену. Но запись кэша — $1090.
   Выясни, СКОЛЬКО РАЗ кэш перестраивался и почему: TTL 5 минут против часового,
   простои между ходами, оживления. Это главный вопрос исследования.
2. Сколько занимает преамбула (`agent::AGENT_SYSTEM_PROMPT` + `agentPreamble`) и растёт ли она.
3. Цена пробуждения цепочки против обычного хода.
4. Какие чаты и типы задач дороже.

## Чего НЕ делать

Не переписывать промпты. Текст промпта — доли процента от входа в сотни тысяч
токенов; методика оптимизации формулировок бьёт по 8.6% выхода, и это надо
подтвердить или опровергнуть числом, а не советом.
